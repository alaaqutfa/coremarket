@extends('backend.layouts.app')
@section('content')
<div class="aiz-titlebar text-left mt-2 mb-3"><h5 class="mb-0 h6">{{ translate('Accounting Core') }}</h5></div>
<div class="alert alert-info">{{ translate('Operational accounting foundation only. This is not a final tax statement or VAT filing.') }}</div>
<div class="card"><div class="card-header"><h6 class="mb-0">{{ translate('Chart of Accounts') }}</h6></div><div class="card-body table-responsive"><table class="table table-bordered mb-0"><thead><tr><th>{{ translate('Code') }}</th><th>{{ translate('Name') }}</th><th>{{ translate('Type') }}</th><th>{{ translate('Normal Balance') }}</th></tr></thead><tbody>@forelse($accounts as $account)<tr><td>{{ $account->code }}</td><td>{{ $account->name }}</td><td>{{ $account->type }}</td><td>{{ $account->normal_balance }}</td></tr>@empty<tr><td colspan="4" class="text-center text-muted">{{ translate('System accounts have not been seeded yet.') }}</td></tr>@endforelse</tbody></table></div></div>
<div class="card"><div class="card-header"><h6 class="mb-0">{{ translate('Journal Entries') }}</h6></div><div class="card-body table-responsive"><table class="table table-bordered mb-0"><thead><tr><th>{{ translate('Number') }}</th><th>{{ translate('Date') }}</th><th>{{ translate('Description') }}</th><th>{{ translate('Debit') }}</th><th>{{ translate('Credit') }}</th><th>{{ translate('Status') }}</th><th></th></tr></thead><tbody>@forelse($journals as $journal)<tr><td>{{ $journal->entry_number }}</td><td>{{ $journal->entry_date }}</td><td>{{ $journal->description }}</td><td>{{ $journal->total_debit }}</td><td>{{ $journal->total_credit }}</td><td>{{ $journal->status }}</td><td><a class="btn btn-soft-primary btn-sm" href="{{ route('operations.accounting.journals.show', $journal) }}">{{ translate('View') }}</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted">{{ translate('No journal entries yet.') }}</td></tr>@endforelse</tbody></table><div class="aiz-pagination">{{ $journals->links() }}</div></div></div>
<div class="card"><div class="card-header"><h6 class="mb-0">{{ translate('VAT Core') }}</h6></div><div class="card-body"><p>{{ translate('Default price mode') }}: {{ config('coremarket.vat.default_price_mode', 'exclusive') }}</p><p>{{ translate('Configured tax rates') }}: {{ $taxRates->count() }}</p><code>php artisan coremarket:vat-audit</code></div></div>
@endsection
