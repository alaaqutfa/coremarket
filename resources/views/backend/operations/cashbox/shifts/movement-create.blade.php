@extends('backend.layouts.app')

@section('content')
<div class="aiz-titlebar text-left mt-2 mb-3"><h5 class="mb-0 h6">{{ translate('Add Cash Movement') }}: {{ $shift->cashbox?->name }}</h5></div>
<div class="alert alert-info">{{ translate('Only manual cash in, cash out, and adjustment movements are available here. No accounting journal is created.') }}</div>
<div class="card"><div class="card-body"><form method="POST" action="{{ route('operations.cash-movements.store', $shift) }}">@csrf<div class="row gutters-10"><div class="col-md-4 form-group"><label>{{ translate('Type') }}</label><select class="form-control" name="movement_type"><option value="cash_in" @selected(old('movement_type') === 'cash_in')>{{ translate('Cash In') }}</option><option value="cash_out" @selected(old('movement_type') === 'cash_out')>{{ translate('Cash Out') }}</option><option value="adjustment" @selected(old('movement_type') === 'adjustment')>{{ translate('Adjustment') }}</option></select></div><div class="col-md-4 form-group"><label>{{ translate('Direction') }}</label><select class="form-control" name="direction"><option value="in" @selected(old('direction') === 'in')>{{ translate('In') }}</option><option value="out" @selected(old('direction') === 'out')>{{ translate('Out') }}</option><option value="neutral" @selected(old('direction') === 'neutral')>{{ translate('Neutral') }}</option></select><small class="text-muted">{{ translate('Cash in must use In and cash out must use Out.') }}</small></div><div class="col-md-4 form-group"><label>{{ translate('Amount') }}</label><input required min="0.000001" step="0.000001" type="number" class="form-control" name="amount" value="{{ old('amount') }}"></div><div class="col-md-6 form-group"><label>{{ translate('Occurred At') }}</label><input type="datetime-local" class="form-control" name="occurred_at" value="{{ old('occurred_at') }}"></div><div class="col-md-12 form-group"><label>{{ translate('Description') }}</label><textarea required class="form-control" name="description">{{ old('description') }}</textarea></div></div><button class="btn btn-primary">{{ translate('Record Movement') }}</button><a class="btn btn-light" href="{{ route('operations.cash-shifts.show', $shift) }}">{{ translate('Cancel') }}</a></form></div></div>
@endsection
