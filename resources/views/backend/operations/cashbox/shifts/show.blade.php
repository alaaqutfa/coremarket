@extends('backend.layouts.app')

@section('content')
<div class="aiz-titlebar text-left mt-2 mb-3"><div class="row"><div class="col"><h5 class="mb-0 h6">{{ translate('Cashier Shift') }} #{{ $shift->id }}</h5></div><div class="col text-right">@if($shift->isOpen() && $canCreateMovement)<a class="btn btn-soft-primary" href="{{ route('operations.cash-movements.create', $shift) }}">{{ translate('Add Cash Movement') }}</a>@endif @if($shift->isOpen() && $canCloseShift)<a class="btn btn-primary" href="{{ route('operations.cash-shifts.close.form', $shift) }}">{{ translate('Close Shift') }}</a>@endif</div></div></div>
<div class="card mb-3"><div class="card-body"><div class="row"><div class="col-md-3"><small class="text-muted">{{ translate('Cashbox') }}</small><div>{{ $shift->cashbox?->name }}</div></div><div class="col-md-2"><small class="text-muted">{{ translate('Status') }}</small><div>{{ translate(ucfirst($shift->status)) }}</div></div><div class="col-md-2"><small class="text-muted">{{ translate('Opening') }}</small><div>{{ number_format((float) $shift->opening_balance, 2) }}</div></div><div class="col-md-2"><small class="text-muted">{{ translate('Expected') }}</small><div>{{ number_format((float) $calculatedExpectedCash, 2) }}</div></div><div class="col-md-3"><small class="text-muted">{{ translate('Actual / Difference') }}</small><div>{{ $shift->actual_cash === null ? '-' : number_format((float) $shift->actual_cash, 2) }} / {{ $shift->cash_difference === null ? '-' : number_format((float) $shift->cash_difference, 2) }}</div></div></div></div></div>
<div class="card"><div class="card-header"><h6 class="mb-0">{{ translate('Cash Movements') }}</h6></div><div class="card-body table-responsive"><table class="table aiz-table mb-0"><thead><tr><th>{{ translate('Date') }}</th><th>{{ translate('Type') }}</th><th>{{ translate('Direction') }}</th><th>{{ translate('Amount') }}</th><th>{{ translate('Description') }}</th><th>{{ translate('Created By') }}</th></tr></thead><tbody>@forelse($shift->movements as $movement)<tr><td>{{ optional($movement->occurred_at)->format('Y-m-d H:i') }}</td><td>{{ translate(ucfirst(str_replace('_', ' ', $movement->movement_type))) }}</td><td>{{ translate(ucfirst($movement->direction)) }}</td><td>{{ number_format((float) $movement->amount, 2) }} {{ $movement->currency }}</td><td>{{ $movement->description ?: '-' }}</td><td>{{ $movement->creator?->name ?: '-' }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted">{{ translate('No cash movements found.') }}</td></tr>@endforelse</tbody></table></div></div>
@endsection
