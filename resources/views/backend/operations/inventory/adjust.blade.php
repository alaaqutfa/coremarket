@extends('backend.layouts.app')
@section('content')
<div class="aiz-titlebar text-left mt-2 mb-3"><h5 class="mb-0 h6">{{ translate('Manual Stock Adjustment') }}</h5></div><div class="card"><div class="card-body"><p><strong>{{ $productStock->product?->name }}</strong> {{ $productStock->variant }} · SKU: {{ $productStock->sku }} · {{ translate('Current quantity') }}: {{ $productStock->qty }}</p><form method="POST" action="{{ route('operations.inventory.stock.adjust.store',$productStock) }}">@csrf <div class="row"><div class="col-md-4 form-group"><label>{{ translate('Adjustment type') }}</label><select class="form-control" name="adjustment_type"><option value="increase">{{ translate('Increase') }}</option><option value="decrease">{{ translate('Decrease') }}</option><option value="set">{{ translate('Set quantity') }}</option></select></div><div class="col-md-4 form-group"><label>{{ translate('Quantity') }}</label><input class="form-control" type="number" step="0.000001" min="0" name="quantity" required></div><div class="col-md-4 form-group"><label>{{ translate('Reason') }}</label><input class="form-control" name="reason" required></div></div><div class="form-group"><label>{{ translate('Notes') }}</label><textarea class="form-control" name="notes"></textarea></div><div class="alert alert-warning">{{ translate('A manual adjustment is recorded in inventory movements. Negative stock is not allowed.') }}</div><button class="btn btn-primary">{{ translate('Record adjustment') }}</button></form></div></div>
@endsection
