@extends('backend.layouts.app')

@section('content')
<div class="aiz-titlebar text-left mt-2 mb-3"><h5 class="mb-0 h6">{{ translate('Loyalty Customer Balances') }}</h5></div><div class="card"><div class="card-body"><form class="row gutters-5 mb-3"><div class="col-md-5"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="{{ translate('Search customer name or email') }}"></div><div class="col-md-3"><select class="form-control" name="status"><option value="">{{ translate('All statuses') }}</option><option value="active" @selected(request('status') === 'active')>{{ translate('Active') }}</option><option value="inactive" @selected(request('status') === 'inactive')>{{ translate('Inactive') }}</option></select></div><div class="col-md-2"><button class="btn btn-primary" type="submit">{{ translate('Filter') }}</button></div></form><div class="table-responsive"><table class="table aiz-table"><thead><tr><th>{{ translate('Customer') }}</th><th>{{ translate('Balance') }}</th><th>{{ translate('Lifetime Earned') }}</th><th>{{ translate('Status') }}</th><th>{{ translate('Last Movement') }}</th><th></th></tr></thead><tbody>@forelse($accounts as $account)<tr><td>{{ $account->user?->name ?: '-' }}<small class="d-block text-muted">{{ $account->user?->email }}</small></td><td>{{ number_format($account->points_balance) }}</td><td>{{ number_format($account->lifetime_points_earned) }}</td><td>{{ translate(ucfirst($account->status)) }}</td><td>{{ $account->movements_max_created_at ?: '-' }}</td><td><a class="btn btn-sm btn-soft-primary" href="{{ route('operations.loyalty.accounts.show', $account) }}">{{ translate('View') }}</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted">{{ translate('No loyalty accounts found.') }}</td></tr>@endforelse</tbody></table></div>{{ $accounts->links() }}</div></div>
@endsection
