@extends('backend.layouts.app')

@section('content')
<div class="aiz-titlebar text-left mt-2 mb-3"><h5 class="mb-0 h6">{{ translate('Loyalty Dashboard') }}</h5></div>
<div class="row gutters-10">@foreach(['accounts' => 'Loyalty Accounts', 'points_balance' => 'Points Balance', 'lifetime_earned' => 'Lifetime Points Earned'] as $key => $label)<div class="col-md-4 mb-3"><div class="card h-100"><div class="card-body"><small class="text-muted">{{ translate($label) }}</small><div class="h4 mb-0">{{ number_format((int) $stats[$key]) }}</div></div></div></div>@endforeach</div>
<div class="card mb-3"><div class="card-header"><h6 class="mb-0">{{ translate('Active Earn Rule') }}</h6></div><div class="card-body">@if($activeRule) {{ $activeRule->name }}: {{ $activeRule->earn_rate_points }} {{ translate('points per') }} {{ $activeRule->earn_rate_amount }} {{ $activeRule->currency }} @else <span class="text-muted">{{ translate('No active loyalty rule.') }}</span> @endif</div></div>
<div class="card"><div class="card-header"><h6 class="mb-0">{{ translate('Recent Loyalty Movements') }}</h6></div><div class="card-body table-responsive"><table class="table aiz-table mb-0"><thead><tr><th>{{ translate('Date') }}</th><th>{{ translate('Customer') }}</th><th>{{ translate('Type') }}</th><th>{{ translate('Points') }}</th><th>{{ translate('Balance After') }}</th></tr></thead><tbody>@forelse($movements as $movement)<tr><td>{{ optional($movement->created_at)->format('Y-m-d H:i') }}</td><td>{{ $movement->user?->name ?: '-' }}</td><td>{{ translate(ucfirst($movement->movement_type)) }}</td><td>{{ $movement->direction === 'out' ? '-' : '+' }}{{ $movement->points }}</td><td>{{ $movement->balance_after }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted">{{ translate('No loyalty movements found.') }}</td></tr>@endforelse</tbody></table></div></div>
@endsection
