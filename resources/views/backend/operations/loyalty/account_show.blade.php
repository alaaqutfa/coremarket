@extends('backend.layouts.app')

@section('content')
<div class="aiz-titlebar text-left mt-2 mb-3"><h5 class="mb-0 h6">{{ translate('Loyalty Account') }}: {{ $account->user?->name }}</h5></div><div class="row"><div class="col-lg-8"><div class="card"><div class="card-body"><div class="row"><div class="col-md-4"><small class="text-muted">{{ translate('Points Balance') }}</small><div class="h4">{{ number_format($account->points_balance) }}</div></div><div class="col-md-4"><small class="text-muted">{{ translate('Lifetime Earned') }}</small><div class="h4">{{ number_format($account->lifetime_points_earned) }}</div></div><div class="col-md-4"><small class="text-muted">{{ translate('Status') }}</small><div class="h4">{{ translate(ucfirst($account->status)) }}</div></div></div></div></div><div class="card"><div class="card-header"><h6 class="mb-0">{{ translate('Movement Ledger') }}</h6></div><div class="card-body table-responsive"><table class="table aiz-table"><thead><tr><th>{{ translate('Date') }}</th><th>{{ translate('Type') }}</th><th>{{ translate('Points') }}</th><th>{{ translate('Balance After') }}</th><th>{{ translate('Reason') }}</th></tr></thead><tbody>@forelse($movements as $movement)<tr><td>{{ optional($movement->created_at)->format('Y-m-d H:i') }}</td><td>{{ translate(ucfirst($movement->movement_type)) }}</td><td>{{ $movement->direction === 'out' ? '-' : '+' }}{{ $movement->points }}</td><td>{{ $movement->balance_after }}</td><td>{{ $movement->reason ?: '-' }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted">{{ translate('No loyalty movements found.') }}</td></tr>@endforelse</tbody></table></div></div></div>
@if($canAdjust)<div class="col-lg-4"><div class="card"><div class="card-header"><h6 class="mb-0">{{ translate('Manual Adjustment') }}</h6></div><div class="card-body"><form method="post" action="{{ route('operations.loyalty.adjust', $account) }}">@csrf <div class="form-group"><label>{{ translate('Points') }}</label><input type="number" name="points" class="form-control" value="{{ old('points') }}" required><small class="text-muted">{{ translate('Use a negative number to remove points.') }}</small></div><div class="form-group"><label>{{ translate('Reason') }}</label><textarea name="reason" class="form-control" maxlength="500" required>{{ old('reason') }}</textarea></div><button class="btn btn-primary" type="submit">{{ translate('Apply Adjustment') }}</button></form></div></div></div>@endif</div>
@endsection
